//<?php
//session_start();
//include('config.php');

// The new password you want to set
$new_password = 'Sabbu@684828/*-'; 

// Generate the hashed password
//$hashed_password = password_hash($new_password, PASSWORD_BCRYPT);

// Prepare the SQL query to update the password for the specific username
//$stmt = $pdo->prepare("UPDATE admins SET password = :password WHERE username = :username");
//$stmt->execute([
 //   'password' => $hashed_password,
 //   'username' => 'dm@maksudulalam.com', // The username whose password needs to be updated
//]);

//echo "Password updated successfully!";
//?>
